package com.example.voicemate.helpers

import android.accessibilityservice.AccessibilityService

object ScrollHelper {
    fun scroll(service: AccessibilityService, direction: String): String {
        // Feature disabled for now
        return "স্ক্রল ফিচারটি বর্তমানে বন্ধ আছে"
    }
}
