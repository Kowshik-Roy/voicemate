package com.example.voicemate.helpers

import android.content.Context

object CallHelper {
    fun makeCall(context: Context, phoneNumber: String): String {
        // Feature disabled for now
        return "কল ফিচারটি বর্তমানে বন্ধ আছে"
    }
}
