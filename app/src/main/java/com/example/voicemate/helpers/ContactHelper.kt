package com.example.voicemate.helpers

import android.content.Context

object ContactHelper {
    fun findNumberByLastThreeDigits(context: Context, lastThree: String): String? {
        // Feature disabled for now
        return null
    }
}
